import { palette } from "./theme";

const tab = {
  text: palette.text,
  background: palette.bg,
  tint: palette.sage,
  tabIconDefault: palette.textMuted,
  tabIconSelected: palette.link,
};

export default {
  light: tab,
  dark: tab,
};
